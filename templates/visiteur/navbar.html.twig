<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<a class="navbar-brand" href="#">GSB Frais</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('saisir') }}">Saisir Fiche Frais</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('choixMois') }}">Consulter Fiche Frais</a>
			</li>
		</ul>
		<ul class="nav navbar-nav navbar-right">
			<li class="dropdown">
				<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.session.get('nom') }}
					{{ app.session.get('prenom') }}</a>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
					<a class="dropdown-item" href="{{ path('personnelles') }}">Données Personnelles</a>
					<a class="dropdown-item" href="{{ path('modifierMdp') }}">Modifier Mot de Passe</a>
					<div class="dropdown-divider"></div>
					<a class="dropdown-item" href="{{path('seDeconnecterVisiteur') }}">Déconnexion</a>
				</div>
			</li>
		</ul>
	</div>
</nav>
